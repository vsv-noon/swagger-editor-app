/*import { z } from "zod";

export const OpenApiSchemaZ= z.object({
  openapi: z.string(),
  info: z.object({
    title: z.string(),
    version: z.string(),
  }),
  paths: z.record(z.string(), z.unknown()),
});

export type OpenApiSchema = z.infer<typeof OpenApiSchemaZ>;*/
